<!--components/area-header/index.wxml-->
<view class="header">
  <view class="title">{{title}}</view>
  <view class="default" wx:if="{{showDefault}}" bindtap="handleRightClick">
    <text>{{rightText}}</text>
    <image class="icon" src="/assets/images/icons/arrow-right.png"></image>
  </view>
</view>